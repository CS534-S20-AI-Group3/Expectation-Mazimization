# Expectation Maximization
Uses expectation maximization to find the ideal clusters for the given data set
To run the program from command line, run: python3 expect_max.py [data filepath] [number of clusters]
